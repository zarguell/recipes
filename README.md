# Recipes

A collection of recipes in `cooklang` format. Many of these recipes are generated by an n8n AI pipeline to convert unstructured Instagram posts into `cooklang` format.

Uses my `cooklang-recipes` repository as an Astro theme to build a static site around my recipe collection.